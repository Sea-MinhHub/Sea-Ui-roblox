--[[
    ╔════════════════════════════════════════════════════════╗
    ║    FULL TEST SCRIPT - SEA MINH UI v2.3.0              ║
    ║    Testing ALL UI Elements & Features                 ║
    ╚════════════════════════════════════════════════════════╝
]]--

local SeaMinhUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Sea-MinhHub/Sea-Ui-roblox/main/Main.lua"))()

-- ============================================================
-- CREATE MAIN WINDOW
-- ============================================================
local window = SeaMinhUI:CreateWindow({
    Title = "Sea Minh UI v2.3.0 - Complete Test",
    Size = UDim2.fromOffset(700, 600),
    Theme = "Dark"
})

-- ============================================================
-- TAB 1: BASIC ELEMENTS
-- ============================================================
local tab1 = window:AddTab({Title = "Basic Elements"})

local section1 = tab1:AddSection("Buttons & Labels")

section1:AddLabel({
    Text = "📌 Welcome to Sea Minh UI v2.3.0!",
    Bold = true,
    Size = 15
})

section1:AddLabel({
    Text = "This is a complete test of all UI elements with SaveManager support.",
    Color = "SubText"
})

section1:AddDivider()

section1:AddButton({
    Title = "Test Button",
    Description = "Click to test button functionality",
    Callback = function()
        print("✓ Button clicked!")
        UI:Notify("Success", "Button works perfectly!", 2)
    end
})

section1:AddButton({
    Title = "Print Current Time",
    Callback = function()
        print("Current time:", os.date("%H:%M:%S"))
    end
})

-- Button Row
section1:AddButtonRow({
    Buttons = {
        {
            Title = "Theme 1",
            Callback = function()
                UI:SetTheme("Dark")
                print("✓ Theme: Dark")
            end
        },
        {
            Title = "Theme 2",
            Callback = function()
                UI:SetTheme("Light")
                print("✓ Theme: Light")
            end
        },
        {
            Title = "Theme 3",
            Callback = function()
                UI:SetTheme("Aqua")
                print("✓ Theme: Aqua")
            end
        }
    }
})

-- ============================================================
-- TAB 2: TOGGLES & CHECKBOXES
-- ============================================================
local tab2 = window:AddTab({Title = "Toggles & Checks"})

local section2 = tab2:AddSection("Toggle Switches")

local toggle1 = section2:AddToggle({
    Title = "Enable Feature 1",
    Description = "Toggle this feature on/off",
    Default = false,
    Callback = function(value)
        print("✓ Toggle 1:", value and "ON" or "OFF")
    end
})

local toggle2 = section2:AddToggle({
    Title = "Auto Save",
    Description = "Automatically save your settings",
    Default = true,
    Callback = function(value)
        print("✓ Auto Save:", value and "ENABLED" or "DISABLED")
    end
})

local toggle3 = section2:AddToggle({
    Title = "Debug Mode",
    Callback = function(value)
        print("✓ Debug Mode:", value and "ON" or "OFF")
    end
})

section2:AddDivider()

local checkSection = tab2:AddSection("Checkboxes")

local check1 = checkSection:AddCheckbox({
    Title = "Enable Notifications",
    Default = true,
    Callback = function(value)
        print("✓ Notifications:", value and "ON" or "OFF")
    end
})

local check2 = checkSection:AddCheckbox({
    Title = "Show FPS Counter",
    Default = false,
    Callback = function(value)
        print("✓ FPS Counter:", value and "VISIBLE" or "HIDDEN")
    end
})

local check3 = checkSection:AddCheckbox({
    Title = "Enable Anti-AFK",
    Default = false,
    Callback = function(value)
        print("✓ Anti-AFK:", value and "ACTIVE" or "INACTIVE")
    end
})

-- ============================================================
-- TAB 3: SLIDERS & INPUTS
-- ============================================================
local tab3 = window:AddTab({Title = "Sliders & Inputs"})

local sliderSection = tab3:AddSection("Slider Controls")

local slider1 = sliderSection:AddSlider({
    Title = "Walk Speed",
    Description = "Adjust player walk speed",
    Min = 16,
    Max = 100,
    Default = 16,
    Rounding = 1,
    Callback = function(value)
        print("✓ Walk Speed:", value)
    end
})

local slider2 = sliderSection:AddSlider({
    Title = "Jump Power",
    Description = "Adjust player jump power",
    Min = 50,
    Max = 200,
    Default = 50,
    Callback = function(value)
        print("✓ Jump Power:", value)
    end
})

local slider3 = sliderSection:AddSlider({
    Title = "FOV",
    Description = "Field of View (degrees)",
    Min = 70,
    Max = 120,
    Default = 90,
    Callback = function(value)
        print("✓ FOV:", value)
    end
})

local inputSection = tab3:AddSection("Text Inputs")

local input1 = inputSection:AddInput({
    Title = "Player Name",
    Description = "Enter your display name",
    Placeholder = "Enter name...",
    Default = "Player",
    Callback = function(value)
        print("✓ Name:", value)
    end
})

local input2 = inputSection:AddInput({
    Title = "Custom Message",
    Placeholder = "Type something...",
    Callback = function(value)
        print("✓ Message:", value)
    end
})

-- Multiline TextBox
inputSection:AddTextBox({
    Title = "Notes",
    Placeholder = "Type your notes here...\nSupports multiple lines!",
    Default = "",
    Callback = function(value)
        print("✓ Notes saved:", value)
    end
})

-- ============================================================
-- TAB 4: DROPDOWNS & COLORS
-- ============================================================
local tab4 = window:AddTab({Title = "Dropdowns & Colors"})

local dropSection = tab4:AddSection("Dropdown Menus")

local dropdown1 = dropSection:AddDropdown({
    Title = "Select Game Mode",
    Description = "Choose your preferred mode",
    Values = {"Solo", "Duo", "Squad", "Arena"},
    Default = "Solo",
    Callback = function(value)
        print("✓ Game Mode:", value)
    end
})

local dropdown2 = dropSection:AddDropdown({
    Title = "Select Weapon",
    Description = "Choose your weapon type",
    Values = {"Sword", "Axe", "Bow", "Staff", "Dagger"},
    Default = "Sword",
    Callback = function(value)
        print("✓ Weapon:", value)
    end
})

local dropdown3 = dropSection:AddDropdown({
    Title = "Select Region",
    Values = {"North America", "Europe", "Asia", "South America", "Oceania"},
    Default = "Asia",
    Callback = function(value)
        print("✓ Region:", value)
    end
})

local colorSection = tab4:AddSection("Color Pickers")

colorSection:AddLabel({
    Text = "Click on color boxes to randomize (demo feature)",
    Color = "SubText"
})

local color1 = colorSection:AddColorPicker({
    Title = "Primary Color",
    Default = Color3.fromRGB(96, 205, 255),
    Callback = function(color)
        print("✓ Primary Color:", color)
    end
})

local color2 = colorSection:AddColorPicker({
    Title = "Secondary Color",
    Default = Color3.fromRGB(255, 100, 100),
    Callback = function(color)
        print("✓ Secondary Color:", color)
    end
})

local color3 = colorSection:AddColorPicker({
    Title = "Accent Color",
    Default = Color3.fromRGB(100, 255, 100),
    Callback = function(color)
        print("✓ Accent Color:", color)
    end
})

-- ============================================================
-- TAB 5: KEYBINDS & ADVANCED
-- ============================================================
local tab5 = window:AddTab({Title = "Keybinds & More"})

local keybindSection = tab5:AddSection("Keybind Settings")

keybindSection:AddLabel({
    Text = "Click on a keybind button and press any key to rebind",
    Color = "SubText"
})

local keybind1 = keybindSection:AddKeybind({
    Title = "Toggle UI",
    Default = Enum.KeyCode.RightControl,
    Callback = function(key)
        print("✓ UI Toggle key:", key.Name)
    end,
    OnPress = function()
        print("✓ UI Toggle pressed!")
    end
})

local keybind2 = keybindSection:AddKeybind({
    Title = "Fly Mode",
    Default = Enum.KeyCode.F,
    Callback = function(key)
        print("✓ Fly key:", key.Name)
    end,
    OnPress = function()
        print("✓ Fly mode toggled!")
    end
})

local keybind3 = keybindSection:AddKeybind({
    Title = "Speed Boost",
    Default = Enum.KeyCode.X,
    Callback = function(key)
        print("✓ Speed key:", key.Name)
    end,
    OnPress = function()
        print("✓ Speed boost activated!")
    end
})

local advancedSection = tab5:AddSection("Advanced Options")

advancedSection:AddLabel({
    Text = "🔥 Advanced Features",
    Bold = true,
    Size = 14
})

advancedSection:AddToggle({
    Title = "Enable Experimental Features",
    Description = "Use at your own risk",
    Default = false,
    Callback = function(value)
        print("✓ Experimental:", value and "ON" or "OFF")
    end
})

advancedSection:AddToggle({
    Title = "Performance Mode",
    Description = "Reduce visual effects for better FPS",
    Default = false,
    Callback = function(value)
        print("✓ Performance Mode:", value and "ON" or "OFF")
    end
})

advancedSection:AddDivider()

advancedSection:AddButtonRow({
    Buttons = {
        {
            Title = "Save",
            Callback = function()
                SaveManager:Save()
                print("✓ Settings saved!")
            end
        },
        {
            Title = "Reset",
            Callback = function()
                SaveManager.Data = {}
                SaveManager:Save()
                print("✓ Settings reset!")
            end
        }
    }
})

-- ============================================================
-- TAB 6: INFORMATION & TESTING
-- ============================================================
local tab6 = window:AddTab({Title = "Info & Testing"})

local infoSection = tab6:AddSection("Information")

infoSection:AddLabel({
    Text = "📊 Sea Minh UI v2.3.0",
    Bold = true,
    Size = 16
})

infoSection:AddLabel({
    Text = "Created with love by Sea Minh Team",
    Color = "SubText"
})

infoSection:AddLabel({
    Text = "Discord: discord.gg/kJqB7R242H",
    Color = "Accent"
})

infoSection:AddDivider()

infoSection:AddLabel({
    Text = "✨ Features:",
    Bold = true
})

infoSection:AddLabel({
    Text = "• Buttons, Toggles, Checkboxes",
    Color = "SubText"
})

infoSection:AddLabel({
    Text = "• Sliders, Inputs, TextBoxes",
    Color = "SubText"
})

infoSection:AddLabel({
    Text = "• Dropdowns, Color Pickers",
    Color = "SubText"
})

infoSection:AddLabel({
    Text = "• Keybinds, Button Rows",
    Color = "SubText"
})

infoSection:AddLabel({
    Text = "• Auto-Save with SaveManager",
    Color = "SubText"
})

infoSection:AddLabel({
    Text = "• 3 Themes (Dark, Light, Aqua)",
    Color = "SubText"
})

local testSection = tab6:AddSection("Testing Tools")

testSection:AddButton({
    Title = "Print All Saved Data",
    Description = "View all saved settings",
    Callback = function()
        print("\n━━━━━━━ SAVED DATA ━━━━━━━")
        for key, value in pairs(SaveManager.Data) do
            print(key, "=", value)
        end
        print("━━━━━━━━━━━━━━━━━━━━━━━━\n")
    end
})

testSection:AddButton({
    Title = "Print All Current Values",
    Description = "View all element values",
    Callback = function()
        print("\n━━━━━━━ CURRENT VALUES ━━━━━━━")
        print("Toggle 1:", toggle1:GetValue())
        print("Toggle 2:", toggle2:GetValue())
        print("Check 1:", check1:GetValue())
        print("Check 2:", check2:GetValue())
        print("Slider 1:", slider1:GetValue())
        print("Slider 2:", slider2:GetValue())
        print("Input 1:", input1:GetValue())
        print("Dropdown 1:", dropdown1:GetValue())
        print("Keybind 1:", keybind1:GetKey().Name)
        print("Color 1:", color1:GetColor())
        print("━━━━━━━━━━━━━━━━━━━━━━━━━━\n")
    end
})

testSection:AddButton({
    Title = "Test All Animations",
    Description = "Trigger hover effects",
    Callback = function()
        print("✓ All animations working!")
        UI:Notify("Success", "Animations tested!", 2)
    end
})

testSection:AddDivider()

testSection:AddButtonRow({
    Buttons = {
        {
            Title = "Destroy UI",
            Callback = function()
                print("✓ Destroying UI...")
                UI:Destroy(window)
            end
        }
    }
})

-- ============================================================
-- STATUS MESSAGES
-- ============================================================
print("\n")
print("╔════════════════════════════════════════════════╗")
print("║   Sea Minh UI v2.3.0 - Full Test Active      ║")
print("║   All UI Elements + SaveManager Ready         ║")
print("╚════════════════════════════════════════════════╝")
print("")
print("✅ Window created successfully")
print("✅ 6 Tabs loaded")
print("✅ 20+ UI Elements ready:")
print("   • Buttons & Button Rows")
print("   • Toggles & Checkboxes")
print("   • Sliders (with decimals)")
print("   • Text Inputs & TextBoxes")
print("   • Dropdowns (multi-option)")
print("   • Color Pickers")
print("   • Keybinds (with OnPress)")
print("   • Labels & Dividers")
print("   • Sections (nested elements)")
print("")
print("🎨 Theme System:")
print("   • Dark (default)")
print("   • Light")
print("   • Aqua")
print("")
print("💾 SaveManager:")
print("   • Auto-save every 3 seconds")
print("   • Persistent data storage")
print("   • Reload-safe settings")
print("")
print("🎮 Test everything:")
print("   1. Click buttons & check console")
print("   2. Toggle switches & checkboxes")
print("   3. Drag sliders (smooth animations)")
print("   4. Type in inputs & textboxes")
print("   5. Select from dropdowns")
print("   6. Click color pickers (randomize)")
print("   7. Set keybinds & press them")
print("   8. Change themes")
print("   9. Close and reopen - data persists!")
print("")
print("Discord: discord.gg/kJqB7R242H")
print("")
